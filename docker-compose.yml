version: "3.9"
services:
  mysql:
    container_name: db-soapservice
    image: mysql:latest
    ports:
      - "3306:3306"
    env_file: .env
    hostname: db-soap-service
    volumes:
      - ./db:/var/lib/mysql
    networks:
      - soap-services

volumes:
  db:
networks:
  soap-services:
